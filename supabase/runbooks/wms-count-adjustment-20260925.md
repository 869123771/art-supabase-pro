# WMS 盘点、调拨与库存调整组装直改记录（2026-09-25）

项目：`ckbftoopuyophiebamwy`。依照本仓库约定，通过 Supabase MCP `execute_sql` 直接执行数据库变更；未创建迁移 SQL 文件。变更前保存了相关函数定义、逻辑结构与菜单记录的本地忽略备份；每组 DDL/RPC 先在 `BEGIN` / `ROLLBACK` 内验证，再正式执行。

## 已上线的库存业务边界

| 业务 | 数据与入口 | 记账规则 |
| --- | --- | --- |
| 分步调拨 | `wms_transfer_document`、`wms_create/dispatch/receive/cancel_transfer_secure`、`/wms/transfer-adjustment/transfer` | 申请、调出、在途、调入；异地仓库收货时选择库位，批次和 SN 的项目施工号保持不变。 |
| 直接调拨 | `wms_inventory_movement`、`wms_post_inventory_movement_secure`、`/wms/transfer-adjustment/direct-transfer` | 同一实物仓库内一步变更库存组织或库位；事务同步扣减来源、增加目标并记录批次和逐件 SN 流水。跨仓库由分步调拨办理。 |
| 盘点 | `wms_count_plan`、`wms_count_line`、`wms_create/record/post/cancel_count_*_secure`、`/wms/transfer-adjustment/count` | 即时库存形成期初快照，逐批保存实盘与 SN，复核后生成盘盈/盘亏流水。记账前检查库存快照是否已变化。 |
| 库存调整 | `wms_adjustment_document`、`wms_post_adjustment_secure`、`/wms/transfer-adjustment/adjustment` | 仓位移动、物料、批次、状态、单位、其他批次属性转换；来源扣减、目标新批次入账，生成 `adjustment_out` / `adjustment_in` 双向流水。 |
| 库存组装 | `wms_assembly_document`、`wms_assembly_component`、`wms_post_assembly_secure`、`/wms/transfer-adjustment/assembly` | 同仓、同组织、同项目施工号和货权的 2–50 个普通批次扣减后生成一个成品批次；每条组件及成品均有库存流水。SN 装配由 MES 工单办理。 |
| 销售发货出库 | SCM `scm_sales_document` 发货通知单、`wms_sales_outbound_allocation`、`wms_post_inventory_movement_secure`、`/wms/receipt-issue/stock-operation` | 已提交通知单按物料行分批出库；每笔关联来源批次、库存流水和 SN。全部明细出库完成后，SCM 通知单自动进入 `shipped`。 |
| MES 完工入库 | MES 报工、确认垛包、`wms_post_inventory_movement_secure`、`/wms/receipt-issue/stock-operation` | 已下达工单先报工，再按未入库量登记生产入库；同一事务生成批次与流水、增加工单累计入库数。已确认垛包按整包块数入库且不可重复办理。 |
| 销售退货入库 | `wms_sales_return_document`、`wms_receive_sales_return_secure`、`/wms/receipt-issue/sales-return` | 关联原销售出库分配，累计退货不得超过该笔出库；保留原项目施工号，原 SN 经退货回库恢复在库，生成退货审计记录及库存流水。 |

批次扩展了生产日期、到期日期、辅助属性、跟踪号、显示单位和显示数量。计量单位转换保持规范库存数量，按 MDM 物料单位换算关系计算目标显示数量。物料转换可以改变目标数量，同时按来源总成本重算目标单位成本。垛包只可整包转换；SN 必须逐件选择，保留两笔流水与同一 SN 的关联记录。调整不能直接改变库存项目归属，跨项目归属必须走项目调拨单。

盘点和调整的业务表对前端只开放受租户范围约束的读取；写入经 `SECURITY DEFINER` RPC、登录身份、租户写入范围与业务按钮权限验证完成。项目施工号关闭时禁止新业务。平台管理员在全部租户视角可基于来源批次确定目标租户；普通用户不能借请求头跨租户操作。

销售出库只能关联已提交的 SCM 发货通知单和其中未出完的物料行。RPC 对租户、物料、项目、施工号、剩余数量、批次可用量和 SN 逐项校验，并在同一事务内写库存流水、出库分配记录与通知单累计出库数量。SCM 的直接“已发货”状态变更以及已有出库后的单据来源字段修改受到数据库触发器拦截。项目发货通知单在提交时必须选择有效施工号；MDM 仓库快捷销售入口跳转至 WMS 出库页，避免调用不带来源单据的旧库存弹窗。出库分配表只开放租户范围内的读取，写入仅由受控 RPC 执行。

MES 旧“交货”仅增加工单数量而不产生库存，现改为跳转 WMS 生产入库；直接调用旧交货 RPC 会收到业务拒绝。生产入库必须带 MES 工单，物料、项目、施工号与工单完全一致，且不得超过已报工未入库量。WMS 工单候选接口支持工单号与 ID 检索，MES 页面跳转可自动定位历史工单。已确认垛包对 WMS 只开放租户范围内的读取，垛包明细提供整包块数；入库锁定垛包、校验块数并阻止重复生产入库。未排包的装配或其他工单仍可按批次入库。MDM 仓库快捷生产入口跳转至 WMS；普通用户只有获得 WMS 查看与入库权限时才会看到相关入口。

WMS 工单领料可按已下达 MES 工单自动带出项目、施工号，也可手动筛选项目施工号与公共库存。来源批次的查询按当前范围过滤；扫码枪回车或手动输入可带入入库批号、逐件录入 SN，出库可按精确批号定位来源库存并逐件选择当前批次 SN。该移动作业入口复用库存作业页面和既有 `WmsStockOperation` 按钮权限。服务端再次校验：关联 MES 工单的出库只能是工单领料，工单必须已下达、未删除；销售出库与直接调拨不能附带工单 ID。正式函数变更前备份于忽略文件 `.artifacts/wms-post-before-issue-guard-20260925.txt`。

直接调拨的目标仓库由来源仓库固定，前端和 RPC 同时拒绝跨仓库一步调拨。每笔直接调拨由 RPC 生成 `DT-日期-随机码` 单号，数据库约束要求单号非空，并以租户及单号唯一索引阻止重复记账。直接调拨列表可按组织、项目、施工号和单号查询；详情展示来源与目标组织、仓库、库位、批次、面积、原因及逐件 SN。数据库函数变更前备份于本地忽略文件 `.artifacts/wms-stock-post-before-direct-transfer-scope-20260925.sql` 和 `.artifacts/wms-stock-post-before-direct-document-20260925.sql`。

垛包读取权限并入 MES 原有的 `tenant_select` 策略，以同一条租户范围谓词授权 MES 排包查看或 WMS 库存作业查看；数据库 advisor 不再报告新增的多条宽松 SELECT 策略。

销售退货按原出库分配单逐笔办理。`wms_sales_return_document` 使用租户范围 RLS，只对已登录账号授予读取；写入仅经 `WmsSalesReturn:Receive` / 原库存入库权限受控 RPC。RPC 锁定原分配，校验累计可退量和退回 SN 是否属于原出库且尚未退过，将原批次项目施工号与板材尺寸、面积比例带到新批次。原 SN 保留编码及父子关系，经合法状态流转重新入库；退货单号在租户内唯一，重试不会重复记账。库存流水以“销售退货入库”展示，仍由原 `other_in` 记账类型参与项目入库统计。新菜单及查看、确认入库按钮各授予既有两类仓储运营角色。

项目施工号报表将累计销售出库、退货回库和净供货分开核算；“净供货”卡片同时显示原累计出库与退货数量。原汇总函数定义备份于忽略文件 `.artifacts/wms-project-summary-before-return-20260925.txt`，退货 RPC 权限扩展前定义备份于 `.artifacts/wms-return-rpc-before-permission-20260925.txt`。

## 验证结果

- 在正式执行前，分步调拨（含 SN 在途）、盘点（含 SN 同量替换）、批次/仓位/状态/物料/单位转换以及项目归属流水均通过事务回滚用例。未保留测试库存或单据。
- 盘点预留数量与出库、分步调拨的项目隔离规则一致：公共预留不会扣占项目专属库存；同项目、同施工号的预留仍参与可用量判定。
- 菜单 `WmsTransfer`、`WmsCount`、`WmsAdjustment` 及对应按钮分别授予既有的两个仓储运营角色；调整菜单与按钮授权数复核为每项两个。
- Data API 校验分步调拨、盘点、调整单及库存物料关联查询均成功；WMS 模块 `vue-tsc`、聚焦 ESLint、`ui:audit`、`permissions:audit`、`reuse:audit` 均通过。真实浏览器检查调整单列表、办理弹窗、1280 px 窄桌面与其他属性转换状态，未见控制台错误或页面横向溢出。
- Supabase advisor 未报告这些新表缺失 RLS 或未索引外键。新索引尚无正式数据查询，`unused_index` 为预期提示。`wms_post_adjustment_secure` 被列为 authenticated 可执行的 SECURITY DEFINER RPC；该入口是有意开放的受权限校验业务接口。参见 [Supabase 对应检查说明](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable)。
- 销售发货出库在正式库以事务回滚验证：直接改 `shipped` 被拒绝；首次出库 1/2 后通知单仍为 `submitted` 且累计出库为 1；第二次出库后状态自动变为 `shipped`，分配、流水和库存扣减一致。项目发货通知单缺施工号被拒绝。回滚后出库分配表为 0 行，没有保留测试库存或单据。
- Data API 验证 SCM 发货通知单与 WMS 出库分配表可按登录权限读取。销售选择页在浏览器以脱敏模拟数据检查通知单、待出物料行、来源批次及 1280 px 页面；无页面脚本错误。新出库分配表没有缺失 RLS 的 advisor 告警，新增索引的 `unused_index` 是未产生正式出库记录时的预期提示。受控的 `wms_post_inventory_movement_secure` 仍有 [authenticated SECURITY DEFINER 提示](https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable)。
- 生产入库及垛包在正式库以事务回滚验证：库存与工单累计入库同步；超报工数量、工单项目不匹配、旧 MES 直接交货、非整包数量、重复垛包入库均被拒绝。回滚后测试批次为 0，工单累计数量和仓库业务配置仍保持原值。垛包与销售出库分配表均验证了平台全部、平台选定、普通用户本租户、普通用户伪造跨租户请求头四种读取边界。
- 浏览器以脱敏模拟数据验证 MES 工单跳转 WMS 后的仓库、工单、物料自动带入，以及垛包选择后批号和数量自动带入；1440 px 与 1280 px 页面无脚本错误。真实角色的整链路记账仍需在对应租户启用成品仓、设置物料入库仓库并完成报工与排包后再执行。
- PDA 尺寸 390 px 浏览器验证工单带出项目施工号、精确批号扫描和预留 SN 逐件扫描；来源查询包含项目 ID 与施工号，页面无横向溢出或控制台错误。待确认工单领料被 RPC 拒绝；已确认工单领料成功写库存流水并扣减库存，测试事务回滚后库存仍为 12、仓库业务范围未改变、未残留测试流水。正式函数与预期定义逐字复核一致。
- 销售退货以正式库事务回滚验证四条路径：普通批次部分退货、项目施工号板材退货、原 SN 主机逐件回库、项目施工号 SN 主机回库。超出原分配数量或重复退回 SN 被拒绝；项目、施工号在目标批次、流水及原 SN 中保持一致。回滚后退货表、测试批次与 SN 均为 0。退货表四种租户读取视角（平台全部、平台选定、普通本租户、伪造跨租户头）通过；新菜单 3 行、角色授权 6 行复核。Data API 的出库分配/退货/流水关联读取通过；浏览器在 1440 px、390 px 检查原出库选择、仓库、SN 扫描及底部记账按钮，无脚本错误或横向溢出。
- 正式库回滚用例验证同一项目施工号销售出库 2 件、退货 1 件后，项目汇总的累计出库、退货与净供货分别为 2、1、1；恢复后正式汇总函数与目标定义一致。退货表的索引覆盖外键检查通过，新增索引尚无正式数据使用，advisor 的 `unused_index` 提示符合预期。
- 项目仓储复盘在 1280 px 与 390 px 浏览器使用脱敏汇总数据验证“净供货”卡片及退货明细提示；390 px 页面为全局移动导航预留宽度后，项目筛选和指标卡不再被图标栏遮挡，页面无横向溢出或脚本错误。
- 直接调拨在正式库事务回滚验证同仓跨组织成功、跨仓一步调拨被拒且库存与流水均不变化、跨仓分步调拨成功；单号自动生成且重复单号被唯一索引拒绝。回滚后没有保留测试单据。列表与详情在浏览器以脱敏模拟数据检查：普通模式按钮位于两个开关右侧，专注模式位于表格左上方；1440 px、390 px 页面无脚本错误和整体横向溢出。测试账号登录与直接 Data API 联调本次遇到本机至 Supabase 的 `ECONNRESET`，因此查询关联根据实际外键定义复核，待网络恢复后需补做在线读取检查。
- 库存组装先在正式库事务回滚验证 SQL 和菜单数据，随后上线审计表、受控 RPC、`WmsAssembly` 页面菜单及 `View`/`Post` 按钮，两个既有仓储角色共 6 条授权。记账回滚用例中两条组件由 8、12 各扣减 1，产生 2 条扣减流水和 1 条成品入库流水；回滚后原库存恢复为 8、12，审计表、流水和临时库位无测试残留。重复组件、库存不足、跨仓库和无权限调用均被拒绝。项目、施工号、库存组织和货权由每条来源一致性校验，目标物料不能是来源物料，垛包与 SN 必须走生产流程，工单预留量在 RPC 中扣除。平台全部、平台选定、本租户普通用户、普通用户伪造租户头和平台切换其他租户的两张审计表读取边界均通过。新增外键索引后，advisor 未报告组装表缺少 RLS 或未索引外键；空表的新索引仍有 `unused_index` 提示。
- 库存组装列表、详情、组件选择与弹窗在 1440 px 和 390 px 的模拟数据浏览器检查通过；普通模式按钮位于两个显示开关右侧，专注模式移至表格左上。`ArtDataSelect` 右上角关闭后无法再次打开的共享状态问题已在公共组件修复；表格单选/多选、树形单选/多选均加入关闭重开回归用例，桌面和移动端通过。组装弹窗改用统一 `ArtEmptyState`，提示条与组件卡片保持 16 px 间距。
- 库存组装浏览器提交检查：选择两条不同来源批次、成品物料与库位，填写数量、面积、长宽厚后，模拟 RPC 收到两条组件及全部成品属性，弹窗完成关闭，浏览器无脚本错误。该检查证明前端提交映射；正式库记账与安全边界由上述事务回滚用例覆盖。组装与直接调拨的本机 Data API 在线关联读取仍受当前网络 `ECONNRESET` 影响，不能据此宣称真实角色的跨模块验收已完成。

## 原始目标闭环核对（继续验收）

- 已有可操作入口与受控记账：SCM 收料通知接 WMS 收货入库，MES 工单报工接生产入库，SCM 发货通知接销售出库，工单关联领料出库，直接/分步调拨，盘点，库存调整，库存组装，销售退货，项目调拨与项目报表。相关交易已分别做回滚验证；这些是分段验证，尚不等于三类业务从下单到供货的整条验收。
- **独立领料申请已接入，整链路仍待验收**：`/wms/receipt-issue/issue-request` 已有申请、审核、分批领料页面和前端 API；线上现有 `wms_issue_request`、`wms_issue_request_line`、`wms_issue_request_allocation` 三表、四个受控 RPC，以及 `WmsIssueRequest` 菜单与五个按钮权限。普通仓储角色在事务回滚用例中完成申请、审核、分两次领料，申请状态由待领料变为部分领料再到已领完，原批次数量、库存流水和申请核销一致；超申请量、跨施工号批次和已领完重复发料均被拒绝。SN 用例验证未选 SN 被拒、逐件领料后 SN 变为出库并绑定工单，核销记录保存同一 SN。回滚后测试申请、批次、流水和 SN 均为 0。三表启用租户范围 RLS，authenticated 仅有 SELECT、anon 无读取；平台全部、平台选定其他租户、普通本租户和普通伪造跨租户请求头读取边界通过。Security Advisor 未报告新增表缺少 RLS 或未索引外键；空表索引显示 unused 符合预期。模拟浏览器验证 1440 px、390 px 申请与分批领料提交、普通/专注按钮位置、无脚本错误或移动端横向溢出。本机直连 Supabase Data API 刷新会话遇到 `ECONNRESET`，真实浏览器关联读取和三类业务共同数据的端到端验收仍需补做。
- **项目工单领用公共库存**：MES 下达工单的自动预留原本按“本项目施工号库存优先，公共库存补足”分配，领料申请此前只允许同项目批次，导致预留与发料范围不一致。现领料申请支持显式切换本项目/公共库存；服务端仍拒绝其他项目或施工号的批次，并由库存过账校验其他工单预留量。以现有项目工单和公共原料批次做事务回滚验收，申请、审核、扣库、流水、申请核销均通过。项目报表的已领料数量和流水页现通过关联工单归集公共库存领料，同时保留流水来源批次的公共归属；回滚用例验证施工号已领料数增加 1、流水显示领用施工号，回滚后无测试单据和库存变化。变更前函数定义保存在本地忽略备份 `.artifacts/wms-issue-post-before-public-fallback-20260925.json` 和 `.artifacts/wms-project-report-before-public-issue-20260925.json`。
- **项目工单领用公共 SN 关键件并绑定主机**：公共批次和 SN 的实际库存归属保持为空；项目工单预留或领用的 SN 通过 `reserved_work_order_id` / `consumed_work_order_id` 取得项目、施工号的业务归集。服务端 `wms_bind_assembly_serials_secure` 与 SN 范围触发器允许“同一已领料工单的公共子件”绑定到该工单的项目主机 SN，继续拒绝其他工单或其他项目归属的子件。项目汇总序列号数量及新建的 `wms_project_serial_page_secure` 同时统计本项目实体 SN 和按工单归集的公共 SN；序列号追溯与项目报表展示有效施工号，公共来源明确标注。使用现有项目工单，在事务内临时建立测试 SN、领料申请和主机绑定，验证领料、父子关系、项目查询及汇总数量后回滚；没有改写真实公共库存的项目字段。变更前函数定义保存在忽略目录 `.artifacts/wms-sn-project-binding-before-20260926.json`。
- **项目序列号读取边界**：新 RPC 仅对已登录且具 `WmsSerialTrace:View` 或 `WmsProjectReport:View` 权限的用户开放，检查项目租户及施工号；`anon` 无执行权，`authenticated` 有执行权且函数内部复核权限，固定空 `search_path`。平台全部、平台选定、本租户普通用户和普通用户伪造租户头均按预期解析读取范围；普通用户查询其他租户项目被拒。当前线上该项目尚无真实项目 SN，四种模式返回 0 是真实数据状态，不代表完整装配业务验收。
- **SN 工单关联索引**：项目追溯需要按预留/领用工单定位公共 SN。`wms_serial_number` 新增 `(reserved_work_order_id, tenant_id)` 非空部分索引，并将既有领用工单索引的列序调整为 `(consumed_work_order_id, tenant_id)`，同时覆盖复合外键查找。变更前索引定义保存在忽略目录 `.artifacts/wms-sn-order-index-before-20260926.json`；事务回滚预演、正式执行和执行后索引定义核对均通过。表当前仅有 2 条 SN，尚无足够数据评估真实查询耗时；数据库性能顾问的全局历史警告不作为该页吞吐量验收。
- **前端验证**：本轮改动文件 Prettier、ESLint、WMS 类型检查、WMS 独立生产构建、UI/权限/复用审计均通过。模拟数据浏览器覆盖 1440 px 与 390 px 的项目报表、序列号列表/详情和公共库存领料选择；无脚本错误或页面级横向溢出。真实业务会话仍受本机到 Supabase Data API 的 `ECONNRESET` 影响；同一项目从关键件 SN 预留到主机发货的真实角色整条验收仍未完成。
- **三类场景的真实角色全链路待验收**：工程项目板材从 SCM 采购与预留到 MES 排包、WMS 入库和按施工号供货；工程项目装配从关键件 SN 预留与领料到主机 SN 绑定、入库、发货；普通非项目业务的公共库存隔离。已有页面和分段 RPC 证据不能代替同一项目/工单/批次贯穿全程的实测。

### 工程板材出入库联动演练（2026-09-26）

用现有项目 `3577c636-b722-406b-8e28-8cf572b409b1`、施工号 `GM202609-01`、工单 `MO202609004` 和垛包 `PK-0001` 做单事务演练。原始垛包未确认、工单完工量为 0；演练在事务内临时将垛包设为已确认、完工量设为 8，并创建一张 SCM 项目发货通知草稿后提交。随后调用正式的 `wms_post_inventory_movement_secure`：整包生产入库 8 块、64㎡，再按同一发货通知行销售出库 8 块、64㎡。结果为生产入库与销售出库各一笔流水，工单累计入库 8、批次在库 0、通知行累计出库 8 且通知单变为 `shipped`，项目施工号汇总净供货 8。执行 `ROLLBACK` 后复核测试批次与通知单均为 0 条、原垛包仍未确认、原工单完工量仍为 0。MES 的报工审批和 SCM 页面点击未在此事务中演练，不能把这次记录称为全业务链真实角色验收。

随后在同一工单上用**正常 MES 业务函数**复演：`mes_submit_production_report` 提交工序良品 8 块，`mes_review_production_report(...,'approve')` 审批后工单完工量变为 8；`mes_save_packing_plan` 保留原 41 个垛包及 41 条明细，只确认 PK-0001。再经 SCM 发货通知草稿提交、WMS 生产入库及销售出库，得到工单完工/入库各 8、SCM 通知行出库 8 且状态 `shipped`、垛包履历排包/入库/出库各 8 块与 64㎡、库存流水 2 条。`ROLLBACK` 后测试报工、通知单、批次均为 0，原工单完工量 0、原垛包未确认。此项证明 MES 报工审批→排包确认→WMS 成品入库→SCM 发货通知→WMS 发货的服务端链路；采购原料与领料、各页面真实账号点击尚未纳入这次事务。

**同一事务从采购收料到项目发货的服务端贯通验收**：在上述既有项目、施工号和工单上，新建 SCM 项目收料通知单（岩棉原料 8）、依业务状态提交及确认、调用 `scm_push_receipt_lines_secure` 下推 WMS 入库草稿并调用 `scm_transition_receipt_target_secure(...,'confirm')`。生成项目/施工号原料批次 8 后，调用 WMS 领料申请创建、审核、按该批次发料；随后依次用 MES 报工提交/审批、排包保存函数确认 PK-0001，WMS 成品整包入库 8 块/64㎡，SCM 项目发货通知提交，WMS 按通知行出库 8 块/64㎡。同一事务的结果：收料单 `completed`、下推入库单 `confirmed`、领料申请 `fulfilled`、报工 `approved`、工单完工/已入库各 8、垛包确认、发货通知 `shipped` 且通知行出库 8；原料与成品批次余额均为 0，流水类型依次为 `receipt_inbound`、`other_out`、`production_in`、`sales_out`。项目施工号汇总已领料 8、累计/净出库 8；垛包履历 PK-0001 排包/入库/出库均为 8 块、64㎡，当前在库 0。整个演练执行 `ROLLBACK`，独立查询确认测试收料单、发货通知、批次、领料申请、已审核报工均为 0，原工单完工量 0、垛包未确认。此项使用了真实业务 RPC 与正常单据状态流转，仍基于既有已下达工单；采购订单分批收料、REL 自动预留和真实角色页面点击尚需单独验收。

**采购订单分批收料专项验收**：在事务中依 SCM 正常状态流转创建并审核一张项目采购订单，单行岩棉原料 20。分别建立关联同一订单明细的两张收料通知，数量 8 和 12；两张通知各自下推、确认 WMS 入库，结果为两张已确认入库单、两个不同批号的项目库存批次、库存总量 20。尝试再次针对同一订单明细收料 1 时，来源单据累计数量校验拒绝“转单数量超过来源单据未转换数量”。回滚后独立查询采购订单、两张收料通知、批次均为 0 条。此项证明分批采购/收料的服务端数量闭环；和 MES 工单从 CRTD 到 REL 的预留仍需另行联动验收。

演练发现原项目汇总中的 `packCount` 是**当前在库垛包数**，全部出库后归零；`库存与垛包` 表也只保留当前批次面积，不能独立承担“项目完工复盘的全部垛包”要求。新增 `public.wms_project_pack_page_secure`，从 MES 工单排包读取项目/施工号下所有垛包，汇总排包块数与面积，并关联 WMS 生产入库、当前在库、销售出库的块数与面积；已全部出库的垛包仍可查询。项目复盘增加“垛包履历”页签，支持包号/工单检索。对同一垛包的回滚验收返回排包 8/64、入库 8/64、在库 0、出库 8/64；未确认的其他垛包仍在列表中，当前施工号共 41 条。

新 RPC 在每个来源表中限定项目租户，通过 `WmsProjectReport:View` 和登录身份复核，并使用空 `search_path`；`anon` 不可执行。平台全部、平台选定、本租户普通用户和伪造租户头均可在授权范围读取 41 条；普通用户查询其他租户项目被拒。事务中临时关闭该施工号后仍可读取 41 条历史垛包，随后回滚。性能上复用工单、排包明细及出入库流水的现有索引，新增批次 `(pack_id,tenant_id)` 非空索引用于垛包履历关联。Security Advisor 对已登录角色可调用受控 `SECURITY DEFINER` RPC 仍发通用告警，入口权限与租户条件已逐项测试；性能顾问把当前 3 行批次表的新索引列为 unused，现阶段没有吞吐量结论。前端类型检查、Prettier、ESLint、权限/UI/复用审计和独立 WMS 生产构建通过；模拟浏览器在 1440 px、390 px 验证已出库/待确认垛包并列展示，无脚本错误或页面级横向溢出。真实业务账号的 SCM→MES→WMS 全链仍需后续验收。

## 运维注意

### WMS 导航与页面目录

WMS 菜单已按 `收发作业`、`调拨盘调`、`库存查询`、`项目仓储` 四个二级目录分组；`仓储工作台` 保留 WMS 一级入口。11 个原业务页面保留 `sys_menu.name`、页面菜单 ID、按钮权限编码与原有角色授权，只变更父级、排序、组件路径。四个新目录的菜单 ID 为 `d1000000-0000-4000-8000-000000000101` 至 `...104`，各按已有子页面授权给相应的 `R_SUPER` 与 `yq_2026_001` 角色。变更前完整菜单与角色授权备份位于本地忽略文件 `.artifacts/wms-menu-before-categories-20260925.json`。

`modules/art-supabase-wms/src/views` 的四个目录与菜单路径一致；WMS 工作台、MDM 仓库与 MES 工单的跨模块入口均已改为新路径。路由守卫会将旧 WMS 书签地址重定向到新目录。库存调整、盘点、分步调拨和项目施工号的主按钮接入 `ArtTableQuery` 工作区操作：普通模式位于两个显示开关右侧，专注模式位于表格顶部左侧。

菜单数据在事务回滚演练后正式写入；复核 5 个 WMS 目录（含顶层）、原有 11 个分类业务页与新增直接调拨、库存组装两个页面。WMS、MDM、MES 类型检查、WMS 生产构建、权限审计、UI 审计、共享复用审计与旧路由单测通过。浏览器 1440 px 验证主按钮切换位置、四组侧栏与页面组件加载；390 px 检查无页面横向溢出，浏览器无脚本错误。`WmsDirectTransfer:View` 查看权限沿用两个仓储运营角色；办理按钮仍使用 `WmsStockOperation:Transfer` 权限。

盘点方案记录期初数量，不锁定仓库业务。盘点期间若库存变化，记账会拒绝并要求重新生成盘点方案。调整和盘点单据为不可直接编辑的审计记录；纠错应走新的业务单据，不更新原库存行的项目字段。上线前应在目标租户的真实权限角色下复核出入库、盘点和调整的完整操作链路。
