# 库位子单元层级（2026-09-24）

- 项目：`ckbftoopuyophiebamwy`
- 规则：库位可挂在同一仓库、同一库区的上级库位下；上级不能是自身或自己的任一下级。展板把子单元显示在上级卡片下；选择上级时排除当前库位及其下级。

## 数据与安全

变更前的 `app_private.validate_warehouse_hierarchy()` 函数定义保存在 `app_private.codex_backup_wms_bin_tree_20260924`，备份 1 行。旧库位共 8 条、子单元 0 条，无既有循环。新规则继续使用原触发器及原有仓库、库区、租户关联校验。父链校验使用仓库级事务锁序列化并发挂接；触发函数仍位于非公开的 `app_private`。

当前登录租户新增 `DEMO-WMS-SH-1-1-A`，作为 `DEMO-WMS-SH-1-1` 的空库存演示子单元，保留供查看。插入前在事务中试写并回滚；原上级库位快照、插入前同编码数量和新子单元 ID 保存在 `app_private.codex_backup_wms_bin_child_demo_20260924`，备份 1 行。演示子单元数量上限为 20，不影响原上级库存。

## 验证与恢复

目标库事务回滚测试及应用后复测均证明：合法父子关系与三级链允许，自身上级、两级循环和三级循环均拒绝。浏览器测试覆盖子单元展示、编辑上级时排除下级、库位悬浮统计、双击库存明细和点击快捷业务。测试产生的临时库位均已回滚。

2026-09-24 再次运行 Supabase 安全和性能顾问。层级触发函数位于非公开的 `app_private`，没有新增针对该函数的安全告警。顾问仍报告仓储公开 RPC 的 `SECURITY DEFINER` 可执行提示、库存表外键索引提示，以及本次私有备份表缺少主键的提示；这些需结合现有权限策略和数据规模单独处理，不属于本次层级规则回归。

恢复时先确认演示子单元无库存、无下级和其他业务引用，再按备份中的 `child_id` 移除演示记录；函数可从 `codex_backup_wms_bin_tree_20260924.hierarchy_definition` 在事务中恢复并验证。保留备份直到仓储模块整体验收。
