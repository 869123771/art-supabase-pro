# 工单下达锁料缺省策略（2026-09-24）

- 项目：`ckbftoopuyophiebamwy`
- 规则：工单从待确认或异常转为已确认时，按 BOM 从可拣货原料仓预留库存；原料不足时拒绝下达并提示缺口。租户在库存策略中显式关闭自动预留后，仍按关闭设置执行。
- 变更：`wms_inventory_policy.auto_reserve_on_release` 的数据库缺省值由 `false` 改为 `true`；`app_private.wms_work_order_reservation_guard()` 在租户尚无策略行时按 `true` 执行。前端无策略行的显示缺省值也同步为开启。

## 备份与验证

变更前将函数定义、列缺省值及策略行快照保存在 `app_private.codex_backup_wms_reservation_default_20260924`。备份核验：1 行、函数定义 5332 字符、原列缺省值 `false`、策略行 0 条。原函数所有者为 `postgres`，`search_path` 为空；变更后保持一致。

先在事务中替换缺省逻辑并回滚，确认列缺省值和函数均恢复原状。再以当前租户的 `DEMO-WMS-001` 待确认演示工单做事务内试下达：无策略行且原料不足时抛出“工单下达失败：原料 …”并阻止状态变更。正式变更后重复测试；临时写入显式 `false` 策略时，试下达可通过且不会新增自动预留。所有试下达、临时策略行和关联写入均已回滚；演示工单仍为待确认，正式策略表仍无行。

正式变更后核验列缺省值为 `true`、触发函数缺省分支为 `true`、备份仍在。Supabase 安全顾问未报告与该函数或策略表相关的新发现；性能顾问对该表仍有原有的多条 permissive RLS 策略提示，本次没有修改 RLS。

恢复时使用备份表中的 `function_definition` 与 `column_default`，先在事务中验证恢复和工单行为，再恢复正式对象；不要删除备份表，直到仓储规则整体验收完成。
