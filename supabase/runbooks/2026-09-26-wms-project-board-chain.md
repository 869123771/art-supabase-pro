# WMS 工程项目板材排包供货链验证

2026-09-26 在 Supabase 项目 `ckbftoopuyophiebamwy` 的 `BEGIN/ROLLBACK` 事务中运行 `.artifacts/wms-board-pack-chain-rollback-20260926.sql`，以聚氨酯外墙板、项目施工号及两块同规格板材核对 SCM、MES、WMS 单据链。

## 验证路径

1. 新建项目施工号和板材工单，保存长 3000 mm、宽 1000 mm、厚 50 mm 的板材明细；工单数量 2 块、面积 6 ㎡。
2. SCM 采购收货使一项 BOM 原料形成带项目施工号的库存批次；工单确认后，WMS 完成预留、领料申请提交审核及实际领料，申请状态为 `fulfilled`。
3. MES 报工 2 块，保存并确认垛包；WMS 生产入库关联该工单与垛包，成品批次保留包号、项目、施工号、尺寸和面积。
4. SCM 发货通知提交后，WMS 按原项目施工号销售出库 2 块，通知状态为 `shipped`。
5. 项目垛包履历按施工号返回这一包：排包 2 块 / 6 ㎡、生产入库 2 块 / 6 ㎡、销售出库 2 块 / 6 ㎡、在库 0；工单状态为 `DLV`。

事务内断言全部通过，结束后 `CHAIN-BOARD-*` 工单与采购/发货单、`CHAIN-PACK-*` 垛包和批次、`CHAIN-WALL-*` 施工号的残留数量均为零。MES 自动排包单元测试 3 项通过，覆盖块数与堆高拆包、人工调整保留、区域轴线禁混包及尾数策略。此数据库验收使用 MES 的工单报工接口和已确认的手工排包方案；工序逐站报工已在装配 SN 链路单独验证。

另用回滚事务验证项目关闭边界：已关闭的施工号和已关闭的项目，均拒绝新增 MES 工单与 WMS 入库，返回“项目或施工号已关闭，不能新增业务”；项目关闭前形成的库存批次在关闭后仍保留数量 1，并可通过项目施工号汇总查询。各测试项目、施工号和批次均已回滚。

再以 `.artifacts/wms-closed-section-sales-out-rollback-20260926.sql` 完成完整板材生产入库链，提交 SCM 发货通知后关闭施工号，再尝试按该施工号销售出库。WMS 返回“项目或施工号已关闭，不能新增业务”；发货通知仍为 `submitted`，成品批次与垛包在库数量仍为 2 块 / 6 ㎡，关闭后的施工号报表仍能查询原工单、垛包和库存。测试事务已回滚。
